version: '3.9'

services:
  db:
    image: mysql
    command: --default-authentication-plugin=mysql_native_password
    restart: always
    env_file:
      - prod.env
    ports:
      - 3306:3306
    expose: 
      - '3306'
    volumes:
      - ./mysql-dump:/docker-entrypoint-initdb.d